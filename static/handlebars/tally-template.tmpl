<span data-hash="{{hash}}" class="remark-tally {{#if inline }}inline{{/if}}">
    <a href="#{{hash}}" data-hash="{{hash}}">
        {{#if count}}
            {{count}}
        {{else}}
            +
        {{/if}}
    </a>
</span>